services:
  whatsapp:
    build: .
    container_name: voicehost-whatsapp-poc
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - whatsapp-data:/app/data

volumes:
  whatsapp-data:
